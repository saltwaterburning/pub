{
	"openapi": "3.0.1",
	"info": {
		"contact": {
			"email": "chuck.marco@prattwhitney.com",
			"name": "Chuck Marco",
			"url": "https://www.dbds.com"
		},
		"description": "DOME Application RESTful API for shop use.",
		"title": "Digital Overhaul Management of Engines",
		"version": "2024-11-11"
	},
	"externalDocs": {
		"description": "Project SharePoint"
	},
	"servers": [
		{
			"url": "http://192.168.3.171:81/dome",
			"description": "DBDS Devlelopment"
		},
		{
			"url": "http://192.168.3.171:82/dome",
			"description": "DBDS Stage"
		},
		{
			"url": "https://dome-ui-test.prattwhitney.com/dome",
			"description": "P&W Stage"
		}
	],
	"security": [
		{
			"bearerAuth": [
			]
		}
	],
	"tags": [
		{
			"description": "MRO (maintenance, repair and operations) Collaboration API.",
			"name": "MRO Collab"
		}
	],
	"paths": {
		"/v1/shop/dome-dates": {
			"post": {
				"description": "Updates the matching DOME record(s) with the provided non-null shop dates. Dates are required to be in ISO 8601 format (yyyy-MM-dd).",
				"operationId": "updateEngineComments",
				"requestBody": {
					"content": {
						"application/json": {
							"schema": {
								"$ref": "#/components/schemas/ShopDatesRequest"
							}
						}
					},
					"required": true
				},
				"responses": {
					"200": {
						"content": {
							"application/json": {
								"examples": {
									"OK": {
										"description": "OK",
										"value": "Success"
									}
								},
								"schema": {
									"$ref": "#/components/schemas/ErrorResponse"
								}
							}
						},
						"description": "Successfully updated the matching DOME record."
					},
					"400": {
						"content": {
							"application/json": {
								"schema": {
									"$ref": "#/components/schemas/ErrorResponse"
								}
							}
						},
						"description": "Invalid request."
					},
					"404": {
						"content": {
							"application/json": {
								"schema": {
									"$ref": "#/components/schemas/ErrorResponse"
								}
							}
						},
						"description": "The matching DOME record was not found."
					}
				},
				"tags": [
					"ShopDates"
				]
			}
		}
	},
	"components": {
		"schemas": {
			"ErrorResponse": {
				"type": "object",
				"description": "Application response schema.",
				"properties": {
					"errors": {
						"type": "array",
						"description": "Chain of application error messages.",
						"items": {
							"type": "string",
							"description": "Chain of application error messages."
						}
					},
					"message": {
						"type": "string",
						"description": "Application message."
					},
					"reason": {
						"type": "string",
						"description": "The HttpStatus Reason Phrase  (e.g., NOT FOUND)."
					},
					"status": {
						"type": "string",
						"description": "The HttpStatus name (e.g., NOT_FOUND)."
					},
					"statusCode": {
						"type": "integer",
						"description": "The HttpStatus value  (e.g., 404).",
						"format": "int32"
					}
				}
			},
			"ShopDatesRequest": {
				"type": "object",
				"properties": {
					"actualCoreAssemblyDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualG1CloseDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualG3CloseDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualG3StartDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualInductDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualKitCompleteDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualReceiptDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualReceiveDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualTestCompleteDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"actualTestStartDate": {
						"type": "string",
						"description": "The new shop date or null if none.",
						"format": "date"
					},
					"engineCenter": {
						"type": "string",
						"description": "The mandatory engine center Id. Example: EC342"
					},
					"engineCenterName": {
						"type": "string",
						"description": "The mandatory engine center name. Example: LHT AERO Alzey"
					},
					"engineSerialNumber": {
						"type": "string",
						"description": "The mandatory ESN."
					},
					"eventId": {
						"type": "integer",
						"description": "The optional eventId.",
						"format": "int32"
					},
					"planAssetCompleteDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planCoreAssemblyDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planGate1CloseDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planGate3CloseDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planGate3StartDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planKitCompleteDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planReceiptDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planReceiveDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planShipDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planTestCompleteDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					},
					"planTestStartDate": {
						"type": "string",
						"description": "The new planned shop date or null if none.",
						"format": "date"
					}
				},
				"required": [
					"engineCenter",
					"engineCenterName",
					"engineSerialNumber"
				]
			}
		},
		"securitySchemes": {
			"bearerAuth": {
				"bearerFormat": "jwt",
				"description": "JWT Bearer Authorization.",
				"scheme": "bearer",
				"type": "http"
			}
		}
	}
}
