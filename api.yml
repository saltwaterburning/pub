openapi: 3.1.0
info:
  contact:
    email: chuck.marco@prattwhitney.com
    name: Chuck Marco
    url: https://www.dbds.com
  description: DOME Application RESTful Forecasting (MML) API.
  title: Digital Overhaul Management of Engines
  version: 2025-01-25
externalDocs:
  description: Project SharePoint
servers:
- description: DBDS Devlelopment
  url: http://192.168.3.171:81/dome
- description: DBDS Stage
  url: http://192.168.3.171:82/dome
- description: P&W Stage
  url: https://dome-ui-test.prattwhitney.com/dome
security:
- bearerAuth: []
tags:
- description: "MRO (maintenance, repair and operations) Collaboration API."
  name: MRO Collab
paths:
  /v1/forecast/annual/le/monthly:
    patch:
      description: Update the annual monthly quantity LE forecast (MML) record(s)
        with any user changes.
      operationId: updateMonthlyPlans_2
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualMonthlyRequestResponse"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh monthly details.
        "201":
          content:
            application/json:
              examples:
                CREATED:
                  description: CREATED
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh monthly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual LE (aka MML)
  /v1/forecast/annual/le/planned-monthly:
    post:
      description: Returns the planned annual LE forecast by month (MML).
      operationId: getMonthlyQtyPlans_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/MmlAnnualMonthlyPlanDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual LE (aka MML)
  /v1/forecast/annual/le/planned-weekly:
    post:
      description: Returns the planned annual LE forecast by week (MML).
      operationId: getWeeklyQtyPlans_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/MmlAnnualWeeklyPlanDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual LE (aka MML)
  /v1/forecast/annual/le/weekly:
    patch:
      description: Update the weekly quantity LE forecast (MML) record(s) with any
        user changes.
      operationId: updateWeeklyPlans_2
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualWeeklyRequestResponse"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh weekly details.
        "201":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh weekly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual LE (aka MML)
  /v1/forecast/annual/mml/monthly:
    patch:
      description: Update the annual monthly quantity MML forecast (MML) record(s)
        with any user changes.
      operationId: updateMonthlyPlans_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualMonthlyRequestResponse"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh weekly details.
        "201":
          content:
            application/json:
              examples:
                CREATED:
                  description: CREATED
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh weekly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual MML (aka MML)
  /v1/forecast/annual/mml/planned-monthly:
    post:
      description: Returns the planned annual MML forecast by month (MML).
      operationId: getMonthlyQtyPlans
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/MmlAnnualMonthlyPlanDetails"
                  example: null
          description: OK
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual MML (aka MML)
  /v1/forecast/annual/mml/planned-weekly:
    post:
      description: Returns the planned annual MML forecast by month (MML).
      operationId: getWeeklyQtyPlans
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/MmlAnnualWeeklyPlanDetails"
                  example: null
          description: OK
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual MML (aka MML)
  /v1/forecast/annual/mml/weekly:
    patch:
      description: Update the annual weekly quantity MML forecast (MML) record(s)
        with any user changes.
      operationId: updateWeeklyPlans_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualWeeklyRequestResponse"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh weekly details.
        "201":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh weekly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual MML (aka MML)
  /v1/forecast/annual/monthly:
    patch:
      description: Update the annual monthly forecast (MML) record(s) with any user
        changes.
      operationId: updateMonthlyPlans
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualMonthlyRequestResponse"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh weekly details.
        "201":
          content:
            application/json:
              examples:
                CREATED:
                  description: CREATED
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh weekly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual (aka MML)
  /v1/forecast/annual/planned-monthly:
    post:
      description: Returns the planned annual monthly forecast (MML) details.
      operationId: getMonthlyAnnualPlans
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/MmlAnnualMonthlyPlanDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual (aka MML)
  /v1/forecast/annual/planned-weekly:
    post:
      description: Returns the planned annual weekly forecast (MML) details.
      operationId: getWeeklyAnnualPlans
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/MmlAnnualWeeklyPlanDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual (aka MML)
  /v1/forecast/annual/weekly:
    patch:
      description: Update the annual planned weekly forecast (MML) record(s) with
        any user changes.
      operationId: updateWeeklyPlans
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MmlAnnualWeeklyRequestResponse"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh weekly details.
        "201":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh weekly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting Annual (aka MML)
  /v1/forecast/le/monthly:
    patch:
      description: Update the monthly LE record(s) with any user changes.
      operationId: updateMonthlyData_1
      parameters:
      - description: Copy to monthly
        in: query
        name: copy-to-monthly
        required: false
        schema:
          type:
          - boolean
          example: null
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingMonthlyUpdateRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh monthly details.
        "201":
          content:
            application/json:
              examples:
                CREATED:
                  description: CREATED
                  value: Void (no body)
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh monthly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting LE (aka MML)
    post:
      description: Returns the monthly LE forecast (MML) details for the provided
        request.
      operationId: getMonthlyData_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingMonthlyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingMonthlyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting LE (aka MML)
  /v1/forecast/le/planned-monthly:
    post:
      description: Returns the planned monthly LE forecast (MML) details for the provided
        request.
      operationId: getMonthlyPlanData_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingMonthlyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingMonthlyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting LE (aka MML)
  /v1/forecast/le/planned-weekly:
    post:
      description: Returns the planned weekly LE forecast (MML) details for the provided
        request.
      operationId: getWeeklyPlanData_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingWeeklyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingWeeklyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting LE (aka MML)
  /v1/forecast/le/weekly:
    patch:
      description: Update the weekly LE record(s) with any user changes.
      operationId: updateWeeklyData_1
      parameters:
      - description: Copy to monthly
        in: query
        name: copy-to-monthly
        required: false
        schema:
          type:
          - boolean
          example: null
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingWeeklyUpdateRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh weekly details.
        "201":
          content:
            application/json:
              examples:
                CREATED:
                  description: CREATED
                  value: Void (no body)
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh weekly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting LE (aka MML)
    post:
      description: Returns the weekly LE forecast (MML) details for the provided request.
      operationId: getWeeklyData_1
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingWeeklyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingWeeklyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting LE (aka MML)
  /v1/forecast/mml/monthly:
    patch:
      description: Update the monthly MML record(s) with any user changes.
      operationId: updateMonthlyData
      parameters:
      - description: Copy to monthly
        in: query
        name: copy-to-monthly
        required: false
        schema:
          type:
          - boolean
          example: null
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingMonthlyUpdateRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              examples:
                OK:
                  description: OK
                  value: Void (no body)
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh monthly details.
        "201":
          content:
            application/json:
              examples:
                CREATED:
                  description: CREATED
                  value: Void (no body)
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh monthly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting MML (aka MML)
    post:
      description: Returns the monthly MML forecast (MML) details for the provided
        request.
      operationId: getMonthlyData
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingMonthlyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingMonthlyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting MML (aka MML)
  /v1/forecast/mml/planned-monthly:
    post:
      description: Returns the planned monthly MML forecast (MML) details for the
        provided request.
      operationId: getMonthlyPlanData
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingMonthlyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingMonthlyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting MML (aka MML)
  /v1/forecast/mml/planned-weekly:
    post:
      description: Returns the planned weekly MML forecast (MML) details for the provided
        request.
      operationId: getWeeklyPlanData
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingWeeklyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingWeeklyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting MML (aka MML)
  /v1/forecast/mml/weekly:
    patch:
      description: Update the weekly MML record(s) with any user changes.
      operationId: updateWeeklyData
      parameters:
      - description: Copy to monthly
        in: query
        name: copy-to-monthly
        required: false
        schema:
          type:
          - boolean
          example: null
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingWeeklyUpdateRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Successfully processed the provided request and no user changes
            detected. Client should not make another request to refresh weekly details.
        "201":
          content:
            application/json: {}
          description: Successfully processed the provided request and user changes
            were detected. Client should make another request to refresh weekly details.
        "400":
          content:
            application/json:
              examples:
                BAD_REQUEST:
                  description: BAD_REQUEST
                  value: ErrorResponse
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting MML (aka MML)
    post:
      description: Returns the weekly MML forecast (MML) details for the provided
        request.
      operationId: getWeeklyData
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForecastingWeeklyRequest"
              example: null
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type:
                - array
                example: null
                items:
                  $ref: "#/components/schemas/ForecastingWeeklyDetails"
                  example: null
          description: Successfully processed the provided request.
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
                example: null
          description: Bad request.
      tags:
      - Forecasting MML (aka MML)
components:
  schemas:
    ErrorResponse:
      type:
      - object
      description: Application response schema.
      example: null
      properties:
        errors:
          type:
          - array
          description: Chain of application error messages.
          example: null
          items:
            type:
            - string
            example: null
        message:
          type:
          - string
          description: Application message.
          example: null
        reason:
          type:
          - string
          description: "The HttpStatus Reason Phrase  (e.g., NOT FOUND)."
          example: null
        status:
          type:
          - string
          description: "The HttpStatus name (e.g., NOT_FOUND)."
          example: null
        statusCode:
          type:
          - integer
          format: int32
          description: "The HttpStatus value  (e.g., 404)."
          example: null
    ForecastingMonthlyDetails:
      type:
      - object
      description: Monthly MML forecasting details.
      example: null
      properties:
        actualReceipentDate:
          type:
          - string
          format: date
          description: EngineTrack actual receive date.
          example: null
        customerName:
          type:
          - string
          description: Engine customer name.
          example: null
        engineCategory:
          type:
          - string
          description: Engine category.
          example: null
        engineCenterName:
          type:
          - string
          description: Engine center name.
          example: null
        engineGroupId:
          type:
          - string
          description: Engine group Id.
          example: null
        engineGroupName:
          type:
          - string
          description: Engine group name.
          example: null
        engineId:
          type:
          - integer
          format: int32
          description: Engine Id.
          example: null
        engineSerialNumber:
          type:
          - string
          description: Engine serial number.
          example: null
        gate3StartDate:
          type:
          - string
          format: date
          description: Gate 3 Start.
          example: null
        month:
          type:
          - integer
          format: int32
          description: Month of the year.
          example: null
        planCompletionDate:
          type:
          - string
          format: date
          description: EngineTracking.engtrackPlanEngComplete.
          example: null
        planCoreDate:
          type:
          - string
          format: date
          description: EngineTracking.enginePlanCoreAssemblyDate.
          example: null
        planId:
          type:
          - integer
          format: int32
          description: DB Plan record Id.
          example: null
        planInductionDate:
          type:
          - string
          format: date
          description: EngineTrack planned induction date.
          example: null
        year:
          type:
          - integer
          format: int32
          description: The selected year.
          example: null
      required:
      - actualReceipentDate
      - customerName
      - engineCategory
      - engineCenterName
      - engineGroupId
      - engineGroupName
      - engineId
      - engineSerialNumber
      - gate3StartDate
      - month
      - planCompletionDate
      - planCoreDate
      - planInductionDate
    ForecastingMonthlyRequest:
      type:
      - object
      description: Request for monthly MML forecasting details.
      example: null
      properties:
        engineCenterId:
          type:
          - string
          description: The selected engine center Id.
          example: null
        engineGroupId:
          type:
          - string
          description: The selected engine group Id.
          example: null
        month:
          type:
          - integer
          format: int32
          description: The month number.
          example: null
          maximum: 12
          minimum: 1
        planCalendar:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planMarket:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planType:
          type:
          - string
          description: The selected PlanType value.
          enum:
          - "0"
          - "1"
          - "2"
          example: null
        year:
          type:
          - integer
          format: int32
          description: The selected plan year.
          example: null
      required:
      - engineCenterId
      - engineGroupId
      - month
      - planCalendar
      - planMarket
      - planType
      - year
    ForecastingMonthlyUpdateRequest:
      type:
      - object
      description: Update request for monthly MML forecasting.
      example: null
      properties:
        engineCenterId:
          type:
          - string
          description: The selected engine center Id.
          example: null
        engineGroupId:
          type:
          - string
          description: The selected engine group Id.
          example: null
        mmlDetails:
          type:
          - array
          description: Collection of monthly MML forecasting details.
          example: null
          items:
            $ref: "#/components/schemas/ForecastingMonthlyDetails"
            example: null
        month:
          type:
          - integer
          format: int32
          description: The month number.
          example: null
          maximum: 12
          minimum: 1
        planCalendar:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planMarket:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planType:
          type:
          - string
          description: The selected PlanType value.
          enum:
          - "0"
          - "1"
          - "2"
          example: null
        year:
          type:
          - integer
          format: int32
          description: The selected plan year.
          example: null
      required:
      - engineCenterId
      - engineGroupId
      - mmlDetails
      - month
      - planCalendar
      - planMarket
      - planType
      - year
    ForecastingWeeklyDetails:
      type:
      - object
      description: Weekly MML forecasting details.
      example: null
      properties:
        actualReceipentDate:
          type:
          - string
          format: date
          description: EngineTrack actual receive date.
          example: null
        customerName:
          type:
          - string
          description: Engine customer name.
          example: null
        engineCategory:
          type:
          - string
          description: Engine category.
          example: null
        engineCenterName:
          type:
          - string
          description: Engine center name.
          example: null
        engineGroupId:
          type:
          - string
          description: Engine group Id.
          example: null
        engineGroupName:
          type:
          - string
          description: Engine group name.
          example: null
        engineId:
          type:
          - integer
          format: int32
          description: Engine Id.
          example: null
        engineSerialNumber:
          type:
          - string
          description: Engine serial number.
          example: null
        gate3StartDate:
          type:
          - string
          format: date
          description: Gate 3 Start.
          example: null
        planCompletionDate:
          type:
          - string
          format: date
          description: EngineTracking.engtrackPlanEngComplete.
          example: null
        planCoreDate:
          type:
          - string
          format: date
          description: EngineTracking.enginePlanCoreAssemblyDate.
          example: null
        planId:
          type:
          - integer
          format: int32
          description: DB Plan record Id.
          example: null
        planInductionDate:
          type:
          - string
          format: date
          description: EngineTrack planned induction date.
          example: null
        week:
          type:
          - integer
          format: int32
          description: Week of the year.
          example: null
        year:
          type:
          - integer
          format: int32
          description: The selected year.
          example: null
      required:
      - actualReceipentDate
      - customerName
      - engineCategory
      - engineCenterName
      - engineGroupId
      - engineGroupName
      - engineId
      - engineSerialNumber
      - gate3StartDate
      - planCompletionDate
      - planCoreDate
      - planInductionDate
      - week
    ForecastingWeeklyRequest:
      type:
      - object
      description: Request for weekly MML forecasting details.
      example: null
      properties:
        engineCenterId:
          type:
          - string
          description: The selected engine center Id.
          example: null
        engineGroupId:
          type:
          - string
          description: The selected engine group Id.
          example: null
        planCalendar:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planMarket:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planType:
          type:
          - string
          description: The selected PlanType value.
          enum:
          - "0"
          - "1"
          - "2"
          example: null
        week:
          type:
          - integer
          format: int32
          description: The week number.
          example: null
          maximum: 52
          minimum: 1
        year:
          type:
          - integer
          format: int32
          description: The selected plan year.
          example: null
      required:
      - engineCenterId
      - engineGroupId
      - planCalendar
      - planMarket
      - planType
      - week
      - year
    ForecastingWeeklyUpdateRequest:
      type:
      - object
      description: Update request for weekly MML forecasting.
      example: null
      properties:
        engineCenterId:
          type:
          - string
          description: The selected engine center Id.
          example: null
        engineGroupId:
          type:
          - string
          description: The selected engine group Id.
          example: null
        mmlDetails:
          type:
          - array
          description: Update request for weekly MML forecasting.
          example: null
          items:
            $ref: "#/components/schemas/ForecastingWeeklyDetails"
            example: null
        planCalendar:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planMarket:
          type:
          - string
          description: The selected PlanCalendar value.
          enum:
          - "0"
          - "1"
          example: null
        planType:
          type:
          - string
          description: The selected PlanType value.
          enum:
          - "0"
          - "1"
          - "2"
          example: null
        week:
          type:
          - integer
          format: int32
          description: The week number.
          example: null
          maximum: 52
          minimum: 1
        year:
          type:
          - integer
          format: int32
          description: The selected plan year.
          example: null
      required:
      - engineCenterId
      - engineGroupId
      - mmlDetails
      - planCalendar
      - planMarket
      - planType
      - week
      - year
    MmlAnnualMonthlyPlan:
      type:
      - object
      example: null
      properties:
        planCounter:
          type:
          - integer
          format: int32
          example: null
        planId:
          type:
          - integer
          format: int32
          example: null
        planMonth:
          type:
          - integer
          format: int32
          example: null
    MmlAnnualMonthlyPlanDetails:
      type:
      - object
      description: Monthly plan details for the year.
      example: null
      properties:
        category:
          type:
          - string
          example: null
        engCenterId:
          type:
          - string
          example: null
        engCenterName:
          type:
          - string
          example: null
        engGroupId:
          type:
          - string
          example: null
        engGroupName:
          type:
          - string
          example: null
        monthlyPlans:
          type:
          - array
          description: Plans by month.
          example: null
          items:
            $ref: "#/components/schemas/MmlAnnualMonthlyPlan"
            example: null
        planCalendar:
          type:
          - string
          description: "The PlanCalendar enumeration. Numeric enum values: 0=CALENDAR,\
            \ 1=FISCAL"
          enum:
          - "0"
          - "1"
          example: null
        planCalendarDescription:
          type:
          - string
          example: null
        planCounterSum:
          type:
          - integer
          format: int32
          example: null
        planMarket:
          type:
          - string
          description: "The PlanMarket enumeration. Numeric enum values: 0=COMMERCIAL,\
            \ 1=AFTERMARKET"
          enum:
          - "0"
          - "1"
          example: null
        planMarketDescription:
          type:
          - string
          example: null
        planType:
          type:
          - string
          description: "The PlanPlanType enumeration. Numeric enum values: 0=INDUCTION,\
            \ 1=SHIPMENT (Completions), 2=REVENUE (Gate3 Start)"
          enum:
          - "0"
          - "1"
          - "2"
          example: null
        planTypeDescription:
          type:
          - string
          example: null
        planYear:
          type:
          - integer
          format: int32
          example: null
        salesOrderType:
          type:
          - string
          example: null
      required:
      - category
      - engCenterId
      - engGroupId
      - monthlyPlans
      - planCalendar
      - planMarket
      - planType
      - planYear
      - salesOrderType
    MmlAnnualMonthlyRequestResponse:
      type:
      - object
      description: Annual plan details by month response.
      example: null
      properties:
        annualPlanDetails:
          type:
          - array
          description: Monthly plan details for the year.
          example: null
          items:
            $ref: "#/components/schemas/MmlAnnualMonthlyPlanDetails"
            example: null
      required:
      - annualPlanDetails
    MmlAnnualRequest:
      type:
      - object
      example: null
      properties:
        engineCenterId:
          type:
          - string
          example: null
        engineGroupId:
          type:
          - string
          example: null
        planCalendar:
          type:
          - string
          description: "The PlanCalendar enumeration. Numeric enum values: 0=CALENDAR,\
            \ 1=FISCAL"
          enum:
          - "0"
          - "1"
          example: null
        planMarket:
          type:
          - string
          description: "The PlanMarket enumeration. Numeric enum values: 0=COMMERCIAL,\
            \ 1=AFTERMARKET"
          enum:
          - "0"
          - "1"
          example: null
        planType:
          type:
          - string
          description: "The PlanPlanType enumeration. Numeric enum values: 0=INDUCTION,\
            \ 1=SHIPMENT (Completions), 2=REVENUE (Gate3 Start)"
          enum:
          - "0"
          - "1"
          - "2"
          example: null
        year:
          type:
          - integer
          format: int32
          example: null
      required:
      - engineCenterId
      - engineGroupId
      - planCalendar
      - planMarket
      - planType
      - year
    MmlAnnualWeeklyPlan:
      type:
      - object
      example: null
      properties:
        planCounter:
          type:
          - integer
          format: int32
          example: null
        planId:
          type:
          - integer
          format: int32
          example: null
        planWeek:
          type:
          - integer
          format: int32
          example: null
    MmlAnnualWeeklyPlanDetails:
      type:
      - object
      description: Weekly plan details for the year.
      example: null
      properties:
        category:
          type:
          - string
          example: null
        engCenterId:
          type:
          - string
          example: null
        engCenterName:
          type:
          - string
          example: null
        engGroupId:
          type:
          - string
          example: null
        engGroupName:
          type:
          - string
          example: null
        planCalendar:
          type:
          - string
          description: "The PlanCalendar enumeration. Numeric enum values: 0=CALENDAR,\
            \ 1=FISCAL"
          enum:
          - "0"
          - "1"
          example: null
        planCalendarDescription:
          type:
          - string
          example: null
        planCounterSum:
          type:
          - integer
          format: int32
          example: null
        planMarket:
          type:
          - string
          description: "The PlanMarket enumeration. Numeric enum values: 0=COMMERCIAL,\
            \ 1=AFTERMARKET"
          enum:
          - "0"
          - "1"
          example: null
        planMarketDescription:
          type:
          - string
          example: null
        planType:
          type:
          - string
          description: "The PlanPlanType enumeration. Numeric enum values: 0=INDUCTION,\
            \ 1=SHIPMENT (Completions), 2=REVENUE (Gate3 Start)"
          enum:
          - "0"
          - "1"
          - "2"
          example: null
        planTypeDescription:
          type:
          - string
          example: null
        planYear:
          type:
          - integer
          format: int32
          example: null
        salesOrderType:
          type:
          - string
          example: null
        weeklyPlans:
          type:
          - array
          description: Plans by week.
          example: null
          items:
            $ref: "#/components/schemas/MmlAnnualWeeklyPlan"
            example: null
      required:
      - category
      - engCenterId
      - engGroupId
      - planCalendar
      - planMarket
      - planType
      - planYear
      - salesOrderType
      - weeklyPlans
    MmlAnnualWeeklyRequestResponse:
      type:
      - object
      description: Annual plan details by week response.
      example: null
      properties:
        annualPlanDetails:
          type:
          - array
          description: Weekly plan details for the year.
          example: null
          items:
            $ref: "#/components/schemas/MmlAnnualWeeklyPlanDetails"
            example: null
      required:
      - annualPlanDetails
  securitySchemes:
    bearerAuth:
      bearerFormat: jwt
      description: JWT Bearer Authorization.
      scheme: bearer
      type: http
